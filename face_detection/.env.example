REDIS_HOST=redis
REDIS_PORT=6379
REDIS_USERNAME=cvsentry
REDIS_PASSWORD=change_me_to_a_strong_password
FACE_PORT=8003
QDRANT_URL=http://qdrant:6333
# ── Pick your runtime ──────────────────────────────────────
# This controls which Dockerfile gets built and which service starts.
# Options: cuda | cpu | openvino
# macOS users: use "cpu" (MPS is not available inside Docker)
COMPOSE_PROFILES=cuda
